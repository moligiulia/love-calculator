#pragma once // Impedisce inclusioni multiple che causerebbero errori di compilazione

// Inclusioni standard del sistema C++
#include <iostream>
#include <string>

/**
 * Nota: In questo progetto Drogon, le classi principali come
 * CalcolatoreController e CompatibilitaEngine sono gestite
 * autonomamente attraverso i loro file header (.h).
 * * Si usa questo file per definire macro globali o versioni del progetto.
 */

#define PROJECT_VERSION "1.0.0"
#define SERVER_PORT 8080